= turbo_frame_tag dom_id(fragment) do
  div class="fragment #{fragment.element}" data-controller="editable" data-action="click->editable#click blur->editable#blur keydown->editable#keydown"
    = fragment.render